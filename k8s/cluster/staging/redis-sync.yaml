apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: url-minifier-redis-sync
  namespace: flux-system
spec:
  interval: 5m
  targetNamespace: url-minifier
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./k8s/base/redis
  prune: true
  decryption:
    provider: sops
    secretRef:
      name: url-minifier-sops-age
