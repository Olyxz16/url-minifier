apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deployment
  namespace: url-minifier
spec:
  template:
    spec:
      containers:
        - name: api
          image: ghcr.io/olyxz16/url-minifier/api:staging-1776200980-bad363b # {"$imagepolicy": "flux-system:url-minifier-image-policy"}
